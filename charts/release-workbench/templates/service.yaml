apiVersion: v1
kind: Service
metadata:
  name: {{ include "release-workbench.fullname" . }}
  labels:
    {{- include "release-workbench.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "release-workbench.selectorLabels" . | nindent 4 }}
